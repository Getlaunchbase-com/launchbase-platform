/**
 * Green Proof Test File
 * 
 * This file contains an intentional TypeScript error for testing
 * the auto-repair system's ability to fix simple type errors.
 */

export function greetUser(name: string): string {
  return `Hello, ${name}!`;
}

export function addNumbers(a: number, b: number): number {
  return a + b;
}
