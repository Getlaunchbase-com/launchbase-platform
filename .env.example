# ═══════════════════════════════════════════════════════════════════════════════
# LaunchBase Platform - Environment Variables
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# ─── Required in Production ─────────────────────────────────────────────────────
# Server will crash without these variables

DATABASE_URL=mysql://dbuser:dbpassword@localhost:3306/launchbase    # REQUIRED in production - MySQL connection string
JWT_SECRET=your-256-bit-secret-key-change-this-in-production    # REQUIRED in production - Auth token signing key (must NOT be "dev-secret-change-in-production")
SESSION_SECRET=your-session-secret-key-at-least-32-characters    # REQUIRED in production - Session cookie signing key
MOBILE_ADMIN_SECRET=your-mobile-admin-secret-key    # REQUIRED in production - Secret for mobile API authentication

# ─── Server Configuration ───────────────────────────────────────────────────────

NODE_ENV=development    # Environment mode: development | production (default: development)
PORT=3000    # HTTP port (default: 3000)
PUBLIC_BASE_URL=http://localhost:3000    # External URL for the application (default: http://localhost:3000)
APP_URL=http://localhost:3000    # Legacy alias for PUBLIC_BASE_URL
ALLOW_STAGING_BYPASS=false    # Enable staging bypass for testing (true/false, default: false)

# ─── Database Configuration (Alternative) ───────────────────────────────────────
# Use these individual fields if not using DATABASE_URL

# DB_HOST=localhost    # Database host
# DB_PORT=3306    # Database port
# DB_USER=dbuser    # Database user
# DB_PASSWORD=dbpassword    # Database password
# DB_NAME=launchbase    # Database name

# ─── Stripe Payment Integration ─────────────────────────────────────────────────

STRIPE_SECRET_KEY=sk_test_51AbCdEfGhIjKlMnOpQrStUvWxYz    # Stripe API secret key (dev passthrough when absent)
STRIPE_WEBHOOK_SECRET=whsec_abcdefghijklmnopqrstuvwxyz123456    # Stripe webhook signature verification secret
SMI_STRIPE_PRICE_ID=price_1AbCdEfGhIjKlMnO    # Price ID for SMI subscription product

# ─── Email Service (Resend) ─────────────────────────────────────────────────────

RESEND_API_KEY=re_abcdefghijklmnopqrstuvwxyz123456    # Resend API key for transactional email
EMAIL_FROM=noreply@launchbase.com    # Sender email address (default: noreply@launchbase.com)

# ─── AI / LLM Services ──────────────────────────────────────────────────────────

OPENAI_API_KEY=sk-proj-abcdefghijklmnopqrstuvwxyz123456789    # OpenAI API key (used for Whisper transcription, AI copy generation)

# ─── Agent Stack ────────────────────────────────────────────────────────────────

AGENT_STACK_URL=http://localhost:4100    # URL of agent-stack runtime (default: http://localhost:4100)
AGENT_SECRET_KEY=your-256-bit-agent-encryption-key-32-chars    # Encryption key for agent instance secrets (AES-256-GCM)

# ─── Facebook Integration ───────────────────────────────────────────────────────

FACEBOOK_PAGE_ID=123456789012345    # Facebook Page ID for posting
FACEBOOK_PAGE_ACCESS_TOKEN=EAABwzLixnjYBO...    # Page-level access token
FACEBOOK_USER_ACCESS_TOKEN=EAABwzLixnjYBO...    # User-level access token (alternative to page token)

# ─── Deployment Providers ───────────────────────────────────────────────────────

VERCEL_TOKEN=your-vercel-deployment-token    # Vercel deployment token
NETLIFY_TOKEN=your-netlify-deployment-token    # Netlify deployment token
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE    # AWS credentials for S3/deployment

# ─── Storage Configuration ──────────────────────────────────────────────────────

ARTIFACTS_DIR=./artifacts    # Local artifact storage directory (default: ./artifacts)
ARTIFACTS_S3_BUCKET=launchbase-artifacts    # S3 bucket name for artifact storage

# ─── Notifications ──────────────────────────────────────────────────────────────

ADMIN_WEBHOOK_URL=https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXX    # Webhook URL for admin alerts
NOTIFICATION_WEBHOOK_URL=https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXX    # Generic notification webhook

# ─── Multi-tenant Configuration ─────────────────────────────────────────────────

OWNER_NAME=default-tenant    # Tenant identifier for multi-tenant deployments
PLATFORM_URL=https://platform.launchbase.com    # External platform URL

# ─── Feature Flags ──────────────────────────────────────────────────────────────

PRESENTATION_TIER=standard    # Design tier: standard | enhanced | premium (default: standard)

# ─── Mobile Configuration ───────────────────────────────────────────────────────

MOBILE_SESSION_TTL_HOURS=24    # Mobile session duration in hours (default: 24)

# ─── Build Metadata (CI Auto-populated) ─────────────────────────────────────────
# These are automatically set by CI/CD systems - DO NOT set manually

# GIT_SHA=abc123def456    # Git commit hash
# GIT_COMMIT=abc123def456    # Alias for GIT_SHA
# BUILD_TIME=2026-02-25T12:00:00Z    # Build timestamp
# BUILD_DATE=2026-02-25T12:00:00Z    # Alias for BUILD_TIME
# APP_VERSION=1.0.0    # Semver version string

# ─── CI Platform Auto-populated ─────────────────────────────────────────────────
# These are automatically set by deployment platforms - DO NOT set manually

# VERCEL_GIT_COMMIT_SHA=abc123def456    # Vercel's git commit SHA
# RENDER_GIT_COMMIT=abc123def456    # Render's git commit hash
# RAILWAY_GIT_COMMIT_SHA=abc123def456    # Railway's git commit SHA
